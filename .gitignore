node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Test output
/coverage

# Plan exports — a runner's own training data, written by scripts/extract-plan.ts
/plan-export

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Package manager: bun only (Vercel picks npm if a package-lock.json is present)
package-lock.json
yarn.lock
pnpm-lock.yaml
